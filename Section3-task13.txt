using System;
using System.Linq;

namespace demo_test
{
    class Program
    {
        static void Main(string[] args)
        {
            int[] Numbers = { 100, 20, 3, 4, 5, 7 };
            Console.WriteLine("maximum no in array is " + Numbers.Max());
            Console.WriteLine("minimum no in array is " + Numbers.Min());

        }


    }
}
